const testNumberCode = () => {
  // // * 1
  // const a = +prompt('first number');
  // const b = +prompt('second number');
  //
  // alert(a + b);

  // * 3
  // const readNumber = () => {
  //   while (true) {
  //     let userInput = prompt('Enter number');
  //
  //     if (isFinite(userInput) && userInput !== null && userInput !== '') {
  //       alert(userInput);
  //       break;
  //     }
  //   }
  // }
  //
  // readNumber();

  // * 5
  // const random = (min, max) => {
  //   return Math.random() * (max - min) + min;
  // }
  //
  // alert(random(15, 25));

  // * 6
  // const random = (min, max) => {
  //   return Math.round(Math.random() * (max - min) + min);
  // }
  //
  // alert(random(15, 25));
}

export default testNumberCode;